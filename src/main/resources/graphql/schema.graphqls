type Query {
    stops: [Stop]
    stopByIds(id: ID): Stop
    userStops: [UserStop]
}

type Mutation {
    addUserStop(stopIds: [String]): UserStop
}

type Stop {
    id: ID
    name: String
    lat: Float
    lon: Float
}

type UserStop {
    id: String
    stopIds: [String]
}
